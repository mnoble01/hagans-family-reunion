import Service from '@ember/service';

export default Service.extend({
  // TODO authentication via airtable!!!
  // Also, need to set environment variables in a hidden place
  // user

  // TODO figure out a browser-based session method
});
